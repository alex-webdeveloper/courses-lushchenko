include header

div(class='wrap')
  div(class='content')
    header(class='site-header')
      include site_header

    div(class='container')
      div(class='row')
    
            
            each val, index in goods
                if val2['id'] == val['category']
                    include card.pug
